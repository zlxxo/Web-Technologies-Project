const assert = require('assert');
var path = require('path');
var fs = require('fs');
const PORT = 3000;

describe('Testiranje - spirala 3', function() {
    it('', function() {
        assert.equal(-1, -1);
    });
});
